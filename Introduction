                                        Introduction
        本项目是笔者学习Narasimha Karumanchi所著的《数据结构与算法经典问题解析》一书过程中，将自己觉得重要的、难点及有意义的一些
知识点的笔记。本项目的所有源码都是基于该书本，然后自己进行扩展。
        以上所述一书是一本优秀的数据结构和算法方面的自学教材，也是一本很好的面试和备考指南。全书以Java为描述语言，介绍计算机编程中使用的
数据结构和算法，强调问题及其分析，而非理论阐述。    



1、什么是递归？  任何调用自身的函数称为递归
2、递归算法的经典用例有哪些？ 斐波那契数列、阶乘、排序、二分查找、树的遍历、图的遍历、动态规划问题、分治算法、汉诺塔及回溯算法等
3、什么是链表？  链表是一种用于存储数据集合的数据结构。 它的主要操作有删除链表、查找、计数、向链表中插入删除元素。链表包括单向链表、双向链表和循环链表

4、什么是栈？  类似于链表，栈也是一种用于存储数据的简单数据结构。它是一种先进后出特征的数据结构。常用操作包括判栈空、判栈满、入栈、出栈
5、栈的实现可以基于静态数组、动态数组和链表来实现      
6、如何用栈来判定括号是否匹配？
        算法：
        1）创建一个栈
        2）当：如果当前字符不是匹配的字符，则忽略它；如果当前字符是一个分隔符（如（，{ 或【），那么就将它入栈；如果字符是一个闭分隔符（如），}，】），且栈不为空
        那么就将栈顶元素出栈，否则提示匹配错误；如果出栈的字符不是匹配的分隔符，则提示匹配出错；
        3）字符串处理结束后，如果栈不为空，则提示匹配出错
7、中缀表达式指运算符左右两边都有字符或表达式，前缀表达式指运算符的前面无字符或表达式，后缀表达式则指运算符的后面无字符或表达式

8、二叉搜索树是一种特殊的二叉树，它的每一个节点的做节点上的数据值都比根节点小，有节点上的数据比跟节点上的数据大。这样一来就方便数据的搜索
      二叉搜索树的主要操作有：*在二叉搜索树中查找元素、查找最小值、查找最大值；插入元素；删除元素；判定一颗树是否为二叉搜索树；寻找树中额第k小的元素；给二叉搜索树中的元素排序等

9、寻找二叉搜索树（BST）中的两个节点的最短路径的算法？只需要在BST树中寻找两个节点的LCA（公共最近邻祖先）即可。。。。。

10、有些情况下，可能需要找到元素集合中的最大值或最小值，可以利用优先队列ADT来完成操作。优先队列是一种数据结构，它支持插入和删除最小值或最大值操作。

11、优先队列的应用：数据压缩（哈如曼编码算法），最短路径算法（德克士特拉算法），最小生成树算法（普利姆算法），事件驱动仿真（顾客排队算法），选择问题（查找第K个最小或最大元素）

12、优先队列的实现方式有多种：基于无序数组实现，无序链表实现，有序数组实现，有序链表实现，二叉搜索树实现，平衡二叉搜索树实现，堆实现

13、堆是一颗具有特殊性质的二叉树。对的基本要求是堆中所有节点的值必须大于或等于（或小于等于）其孩子节点的值，另外一个对的性质是，堆应该是一颗完全二叉树

14、堆的类型有两种，即最大堆（父节点的值必须大于或等于其孩子节点的值）和最小堆（父节点的值必须小于等于其孩子节点的值）

15、二叉堆中，每个节点最多有两个孩子节点。在实际应用中，二叉堆已经足够满足需求。

16、将关键字映射到存储位置的过程叫做散列。散列是一种用于实现信息存储和快速检索的技术。它常用于执行优化搜索和符号表的实现。

17、散列常用的操作：创建一个新的散列表；在散列中搜索关键字；在散列中插入一个新的关键字；删除一个关键字；删除散列表

18、散列有4个主要组成部分：散列表、散列函数、冲突、冲突解决技术

19、散列表是数组的一个推广。当面临较少的存储位置和较多的关键字时，仅用简单的数组是不足以解决问题的，所以就出现了散列表

20、散列表利用散列函数将关键字映射到其关联的值。通用惯例是，当关键字的实际存储数目相对于关键字的取值范围较小时，可以使用散列表

21、散列函数用于将关键字转换成索引。理想情况下，散列函数应该将每个关键字映射到唯一的索引，但现实中很难实现

22、一个好的散列函数的特点：最大限度的减少冲突；简单快速计算；将键值在散列表中均匀分布；能使用关键字提供的所有信息；对一组给定的关键字具有一个高负载因子（
            负载因子决定了散列函数是否能将关键字均匀分布）（冲突是指不能将每个关键字都映射到不同的唯一的地址空间）（寻找替代位置的过程称为冲突解决）
            
23、常用的冲突解决方法有直接连接法（分离链接法）和开放定址法（基于数组实现，主要包括线性探测法、二次探测法和双重散列法）    
           mmmmmmm llllll  sssss

                                                                            